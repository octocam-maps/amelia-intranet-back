# Dependencias de runtime. Espejo de backend2: FastAPI + asyncpg (SQL crudo, sin ORM).
fastapi[standard]>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
asyncpg>=0.29.0
python-jose[cryptography]>=3.3.0
google-auth>=2.28.0
google-api-python-client>=2.120.0
google-auth-httplib2>=0.2.0
requests>=2.31.0
slowapi>=0.1.9
loguru>=0.7.0
httpx>=0.25.0
sendgrid>=6.11.0
openpyxl>=3.1.0
pillow>=10.0.0
reportlab>=4.0.0
# Se necesita para RELLENAR los PDF que entrega RRHH sin re-maquetarlos: el
# documento original se usa como plantilla intacta y `merge_page` le fusiona
# encima una capa de reportlab con los datos del perfil (ver
# `onboarding/infrastructure/signable_documents.py`). reportlab sabe CREAR PDFs,
# no leerlos, así que por sí solo no puede partir de un fichero existente.
pypdf>=6.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.11.1
faker>=19.0.0
